### REQUIRED
# The namespace of the collection. This can be a company/brand/organization or product namespace under which all
# content lives. May only contain alphanumeric lowercase characters and underscores. Namespaces cannot start with
# underscores or numbers and cannot contain consecutive underscores
namespace: jackson1719
name: lab
version: 1.0.0
readme: README.md
authors:
  - jackson1719

### OPTIONAL but strongly recommended
# A short summary description of the collection
description: Roles related to my homelab
license:
  - MIT
license_file: ''

# A list of tags you want to associate with the collection for indexing/searching. A tag name has the same character
# requirements as 'namespace' and 'name'
tags:
  - tautulli
  - airvpn
  - motd

dependencies: {}
repository: https://github.com/jackson1719/ansible-collection-lab
documentation: https://github.com/jackson1719/ansible-collection-lab/wiki
homepage: https://github.com/jackson1719/ansible-collection-lab
issues: https://github.com/jackson1719/ansible-collection-lab/issues

# A list of file glob-like patterns used to filter any files or directories that should not be included in the build
# artifact. A pattern is matched from the relative path of the file or directory of the collection directory. This
# uses 'fnmatch' to match the files or directories. Some directories and files like 'galaxy.yml', '*.pyc', '*.retry',
# and '.git' are always filtered
build_ignore: []

